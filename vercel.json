{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.6.1 --activate && pnpm -r build",
  "installCommand": "corepack enable && corepack prepare pnpm@10.6.1 --activate && pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/dist",
  "framework": null
}
